import { useMemo } from 'react'
import { Link } from 'react-router-dom'
import { FiArrowRight, FiCalendar, FiClock, FiDollarSign, FiPlayCircle, FiUserPlus, FiUsers } from 'react-icons/fi'
import { Line, LineChart, Pie, PieChart, ResponsiveContainer, Tooltip, Cell, XAxis, YAxis } from 'recharts'
import { useAuth } from '../context/AuthContext'
import { getAllAttendance } from '../services/attendanceService'
import { getAllEmployees } from '../services/employeeService'
import { getAllLeaves, getPendingLeaves } from '../services/leaveService'

const chartColors = ['#2563eb', '#14b8a6', '#f59e0b', '#8b5cf6', '#f43f5e', '#64748b']

/** Render the AttendEase dashboard overview. @returns {JSX.Element} Dashboard page. */
export default function DashboardPage() {
  const { user } = useAuth()
  const employees = getAllEmployees()
  const attendance = getAllAttendance()
  const presentToday = attendance.filter((record) => record.date === new Date().toLocaleDateString('en-CA')).length
  const onLeave = getAllLeaves().filter((leave) => leave.status === 'approved' && new Date().toLocaleDateString('en-CA') >= leave.startDate && new Date().toLocaleDateString('en-CA') <= leave.endDate).length
  const pendingApprovals = getPendingLeaves().length
  const attendanceTrend = useMemo(() => Array.from({ length: 7 }, (_, index) => { const date = new Date(); date.setDate(date.getDate() - (6 - index)); const key = date.toLocaleDateString('en-CA'); return { day: date.toLocaleDateString([], { weekday: 'short' }), present: attendance.filter((record) => record.date === key).length } }), [attendance])
  const departmentDistribution = useMemo(() => Object.entries(employees.reduce((groups, employee) => { groups[employee.department] = (groups[employee.department] || 0) + 1; return groups }, {})).map(([name, value]) => ({ name, value })), [employees])
  const recentActivity = [...attendance].sort((a, b) => String(b.date).localeCompare(String(a.date))).slice(0, 5)
  const stats = [['Total Employees', employees.length, FiUsers, 'text-blue-600 bg-blue-50'], ['Present Today', presentToday, FiClock, 'text-emerald-600 bg-emerald-50'], ['On Leave', onLeave, FiCalendar, 'text-amber-600 bg-amber-50'], ['Pending Approvals', pendingApprovals, FiUserPlus, 'text-violet-600 bg-violet-50']]

  return <div className="space-y-6"><section className="overflow-hidden rounded-2xl bg-gradient-to-br from-blue-700 via-blue-800 to-slate-950 p-6 text-white shadow-xl sm:p-8"><p className="text-sm font-semibold uppercase tracking-widest text-blue-200">Your workspace</p><h1 className="mt-3 text-3xl font-bold tracking-tight sm:text-4xl">Welcome back, {user?.fullName || user?.username || 'there'}!</h1><p className="mt-3 max-w-xl text-blue-100">Here is what is happening across your team today.</p></section><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{stats.map(([label, value, Icon, color]) => <article className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm" key={label}><div className="flex items-start justify-between"><div><p className="text-sm text-slate-500">{label}</p><p className="mt-3 text-2xl font-bold text-slate-900">{value}</p></div><span className={`rounded-lg p-3 ${color}`}><Icon size={20} /></span></div></article>)}</div><div className="grid gap-6 xl:grid-cols-[1.45fr_1fr]"><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><div><h2 className="font-bold text-slate-900">Attendance trend</h2><p className="mt-1 text-sm text-slate-500">Last 7 days</p></div><FiClock className="text-blue-600" /></div><div className="mt-5 h-64"><ResponsiveContainer width="100%" height="100%"><LineChart data={attendanceTrend}><XAxis dataKey="day" tickLine={false} axisLine={false} /><YAxis allowDecimals={false} tickLine={false} axisLine={false} /><Tooltip /><Line dataKey="present" name="Attendance" stroke="#2563eb" strokeWidth={3} dot={{ r: 4 }} /></LineChart></ResponsiveContainer></div></section><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-bold text-slate-900">Department distribution</h2><div className="mt-3 h-64"><ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={departmentDistribution} dataKey="value" nameKey="name" innerRadius={55} outerRadius={85} paddingAngle={3}>{departmentDistribution.map((entry, index) => <Cell fill={chartColors[index % chartColors.length]} key={entry.name} />)}</Pie><Tooltip /></PieChart></ResponsiveContainer></div><div className="flex flex-wrap justify-center gap-x-4 gap-y-2 text-xs text-slate-500">{departmentDistribution.map((entry, index) => <span className="flex items-center gap-1" key={entry.name}><i className="h-2 w-2 rounded-full" style={{ backgroundColor: chartColors[index % chartColors.length] }} />{entry.name}</span>)}</div></section></div><div className="grid gap-6 xl:grid-cols-[1.45fr_1fr]"><section className="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><div className="flex items-center justify-between border-b border-slate-100 px-5 py-4"><h2 className="font-bold text-slate-900">Recent activity</h2><Link className="text-sm font-semibold text-blue-600" to="/attendance">View all <FiArrowRight className="inline" /></Link></div><div className="overflow-x-auto"><table className="min-w-[620px] w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="px-5 py-3">Employee</th><th className="px-5 py-3">Date</th><th className="px-5 py-3">Clock in</th><th className="px-5 py-3">Status</th></tr></thead><tbody className="divide-y divide-slate-100">{recentActivity.map((record) => <tr key={record.id}><td className="px-5 py-4 font-medium text-slate-800">{record.employeeName}</td><td className="px-5 py-4 text-slate-500">{record.date}</td><td className="px-5 py-4 text-slate-500">{record.clockIn}</td><td className="px-5 py-4 capitalize text-slate-600">{record.status}</td></tr>)}</tbody></table></div></section><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-bold text-slate-900">Quick actions</h2><div className="mt-4 space-y-3"><QuickAction icon={FiPlayCircle} label="Clock In" to="/attendance" /><QuickAction icon={FiCalendar} label="Apply Leave" to="/leave" /><QuickAction icon={FiDollarSign} label="Generate Payroll" to="/payroll" /></div></section></div></div>
}

function QuickAction({ icon: Icon, label, to }) { return <Link className="flex items-center justify-between rounded-lg border border-slate-200 px-4 py-3 text-sm font-semibold text-slate-700 transition hover:border-blue-200 hover:bg-blue-50 hover:text-blue-700" to={to}><span className="flex items-center gap-3"><Icon className="text-blue-600" />{label}</span><FiArrowRight /></Link> }
